# --- Phase 1: core (backbone + prototypes) ---
torch>=2.7.1
torchvision>=0.20.0
numpy>=1.26

# --- will be needed starting Phase 1 step 3 (capture/enroll/live) ---
opencv-python>=4.9

# --- Phase 2: visual design system (typography, glass panels, themes) ---
# Pillow is also a transitive torchvision dependency, but ui.py imports it
# directly for text rendering, so it's pinned explicitly rather than relying
# on that being true forever.
Pillow>=10.0

# --- Phase 2: ambient audio + SFX (fail-soft — see protovision/audio.py) ---
pygame>=2.5

# --- dev / testing ---
pytest>=8.0
